
trapworks_strings = {
	translator = core.get_translator("trapworks_strings")
}

local modpath = core.get_modpath(core.get_current_modname())

dofile(modpath.."/buildings.lua")

dofile(modpath.."/string.lua")
