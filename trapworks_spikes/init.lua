
trapworks_spikes = {
	translator = core.get_translator("trapworks_spikes")
}

local modpath = core.get_modpath(core.get_current_modname())

dofile(modpath.."/buildings.lua")

dofile(modpath.."/spikes.lua")
