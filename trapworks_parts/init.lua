trapworks_parts = {
	translator = core.get_translator("trapworks_parts")
}

local modpath = core.get_modpath(core.get_current_modname())

dofile(modpath.."/crafting.lua")

dofile(modpath.."/items.lua")
